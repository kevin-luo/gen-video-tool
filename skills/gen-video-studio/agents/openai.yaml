interface:
  display_name: "Gen Video Studio"
  short_description: "Generate local videos directly from a Codex conversation"
  default_prompt: "Use $gen-video-studio to turn this finished script into a local video with narration and sidecar SRT."
